<%= form_for(@dog_breed) do |f| %>
  <% if @dog_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog_breed.errors.count, "error") %> prohibited this dog_breed from being saved:</h2>

      <ul>
      <% @dog_breed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :dog_breed_group_id %><br>
    <%= f.text_field :dog_breed_group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
