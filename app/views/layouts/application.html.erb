<!DOCTYPE html>
<html>
<head>
  <title>Dog Score Keeper</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%#= stylesheet_link_tag params[:controller] %>
  <%#= yield :head %>
  <%#= yield :action_specific_css if content_for?(:action_specific_css %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<!-- Fixed navbar -->
<div id="dsk-nav-bar" role="navigation" class="navbar navbar-inverse navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Dog Score Keeper', root_path, :class=>'navbar-brand' %>
      <!--<a href="" class="navbar-brand">Dog Score Keeper</a>-->
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(features_path)%>"><%= link_to 'Features', features_path %></li>
        <li class="dropdown">
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">More <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><a href="#">Blog</a></li>
            <li><a href="#">About Us</a></li>
          </ul>
        </li>
      </ul>


      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if current_page?(supports_path)%>"><%= link_to 'Help & Support', supports_path %></li>
        <% unless user_signed_in? -%>
            <li class="<%= 'active' if current_page?(new_user_session_path)%>"><%= link_to 'Sign in', new_user_session_path %></li>
        <% else -%>
            <li class="dropdown">
              <a data-toggle="dropdown" class="dropdown-toggle" href="#"><%= current_user.email %> <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li class="<%= 'active' if current_page?(user_root_path)%>"><%= link_to 'Dashboard', user_root_path%></li>
                <li><a href="#">Account</a></li>
                <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>

              </ul>
            </li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>

<div id="dsk-container" class="container">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <div id="content">
    <%= content_for?(:content) ? yield(:content) : yield %>
  </div>

</div> <!-- /container -->




</body>
</html>
